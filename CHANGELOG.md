# 更新日志

## 0.21.0-beta.6 — 2026-08-28

这是修复长时间待机后鼠标交互完全失效的本地测试版更新。

### 修复

- 修复长时间待机（锁屏/屏保/全屏覆盖）恢复后，点击、摸头、戳脸、呼出右键菜单全部失效的问题：午睡会话在挂起期间被孤立，恢复后其循环片段已被空闲片段替换，唤醒接缝永远无法到来，所有交互被唤醒延迟门静默吞掉；现在挂起恢复会强制终结孤立会话并补执行被吞的交互。
- 修复拖拽中进入待机后角色永久粘在光标上的问题：挂起最小化会吞掉鼠标释放事件，现在挂起时丢弃按压状态，并在每帧校验物理按键状态，发现释放事件丢失时立即取消按压。
- 修复托盘"显示小千寻"和"回到中央"在挂起状态下恢复窗口后，角色可见但完全不响应点击的问题：显式显示请求现在会同步退出挂起状态（若全屏窗口仍在前台，400ms 系统检查会自动重新挂起）。

### 验证

- Godot 4.7.1 自动回归：1226 条断言全部通过（新增 12 条待机恢复与按压看门狗回归测试）。
- 主场景完成短时加载检查，Windows x86_64 发行版成功导出，文件版本为 `0.21.0.6`。

## 0.21.0-beta.5 — 2026-08-16

这是加入前景高窗自主长跳与完整意外恢复的本地测试版更新，并继续包含 beta.4 的窗口层级误坠落修复。

### 新增

- 新增独立自主表现 `window_hop_up`：角色站在窗口上时，会在候选稳定 2 秒后择机评估 Windows 原生 Z-order 中最前方的高处小窗。
- 目标须提供至少 160 像素真实可见顶边，水平距离不超过 900 像素、上升 16–120 像素；统一复用走路、转身、起跳、腾空、落地和窗口落地恢复动作。
- 新行为要求至少 35 点能量，使用独立 45 秒冷却，并由好奇心与无聊度影响选择权重。

### 调整

- 保留现有随机窗口跃迁的候选算法、45–90 秒计时、60% 触发率和通用飞行路径；新长跳不会重置其计时或替换其表现。
- 新目标获取使用严格的原生窗口可见片段；既有站立支撑继续遵守 beta.4 的宠物置顶层级规则，两套语义相互独立。
- 调试信息增加长跳阶段、目标身份和结束原因。
- 桌面图标自主条件改为一次 Explorer 枚举同时计算可见性与可达性；巡航、飞行和爬墙期间复用最近的空闲快照，避免同步跨进程查询造成动作小卡顿。

### 意外恢复

- 起跳前目标关闭、最小化、最大化、失去最前层资格、被遮挡、瞬移或 HWND/PID 身份变化时取消，并继续保留有效源窗口支撑；单次查询缺失保留 250ms 宽限。
- 起跳动画完成后会重新执行一次原生 Z-order 枚举；若用户刚切换了前景窗口，会在释放源窗口前取消，不再使用最长 500ms 的缓存层级起跳。
- 长跳会话使用“走到起跳点所需时间 + 8 秒动作余量”的动态期限；任何动画完成事件缺失都能按当前阶段收口为源窗口、自然坠落或有效目标平台。
- 起跳前源窗口消失时保留既有平台丢失流程已经计算好的坠落时长、雨伞判定与扫掠落地，不再退化为固定 420ms 坠落。
- 起跳后锁定同一目标，目标移动或缩放时只在同一 HWND 的真实可见顶边内重算落点；目标失效后保留横向惯性转为自然下降。
- 下降使用扫掠落地，优先接住路径上的首个真实窗口平台，没有平台则落到地面，高落差继续使用雨伞。
- 拖拽和手动操控仍拥有最高优先级；菜单、背包和全屏会在起跳前取消，在空中则交接给安全坠落。

### 验证

- Godot 4.7.1 自动回归：1216 条断言全部通过。
- 主场景完成短时加载检查，Windows x86_64 发行版成功导出，文件版本为 `0.21.0.5`。

## 0.21.0-beta.4 — 2026-08-16

这是修正置顶角色与后台最大化窗口层级判定的本地测试版更新。

### 修复

- 修复角色站在前景小窗口上时，被其后方的最大化应用误判为遮挡并坠落的问题。
- 最大化窗口现在只有在 Z-order 确实位于角色窗口前方且覆盖脚下接触点时才会撤销支撑。
- 将 Z-order `0` 作为有效的最顶层位置处理，不再错误退回旧的全遮挡判定。

### 验证

- Godot 4.7.1 自动回归：1172 条断言全部通过。
- Windows x86_64 发行版成功导出，文件版本为 `0.21.0.4`。

## 0.21.0-beta.3 — 2026-08-15

这是在 `0.21.0-beta.2` 基础上补充窗口立足修复与精力休息反馈的测试版更新。

### 调整

- 角色每次启动时精力回满至 100；精力越低，越倾向选择地面坐下、窗口坐下或睡眠，但不会强制过滤其他自主行为。
- 地面坐下和窗口坐下仅在循环阶段按每分钟 8 点恢复精力；睡眠循环按每分钟 12 点恢复精力。
- 移除坐下动作结束时的一次性精力奖励；进入与退出动画不恢复精力，中断时只保留实际休息时间产生的恢复。
- 睡眠恢复到 60 点后等待动画循环边界，再完整播放唤醒动作并返回空闲状态。

### 修复

- 修复角色站在小窗口上时，展开最大化窗口后仍停留在原高度的虚空站立问题。
- 区分普通瞬时遮挡与最大化覆盖：普通弹窗和屏幕边缘裁切不会误把角色挤落；最大化窗口覆盖脚下接触点时立即失去支撑。
- 手动操控和自主爬窗使用同一最大化遮挡规则，不再经过 1.5 秒遮挡宽限，也不会在退出操控时重新绑定到已被覆盖的小窗。

### 验证

- Godot 4.7.1 自动回归：1167 条断言全部通过。
- Windows x86_64 发行版成功导出，并完成真实窗口与角色可见性验收。

## 0.21.0-beta.2 — 2026-08-12

这是一次以桌面生态互动、行为公平性和养成反馈为核心的测试版更新。

### 新增

- 新增公平自主调度器，在生态日程、单次行为和自由移动之间分配执行机会，并为长期未执行的候选提供等待补偿与最终保底。
- 新增好奇与无聊驱动的光标玩耍，以及烦躁驱动的两次警告、第三次绝对没收流程。
- 完善五阶段亲密度养成：疏远、戒备、熟悉、信任、亲近；加入每日变化额度、迟滞阈值和关系专属互动台词。
- 新增桌面图标拖动赠予、背包容量拒绝、定时收纳、藏品延长保留、自动随机放置、单项要回与一键还原。
- 机制面板增加自主调度、关系变化、桌面图标与光标惩罚链的运行信息。

### 调整

- 桌面图标背包上限为 3；所有收纳图标在保留期间从桌面渲染层隐藏，正常退出时恢复仍在背包中的图标。
- 普通图标保留 60–120 秒，藏品保留 180–300 秒；自动放置遵循可见空网格与全局冷却。
- 光标没收期间角色可继续普通行动；释放时从提包位置恢复光标。Esc 优先归还被没收的光标，否则用于快速退出手动操控。
- 玩家称谓统一为人格化的“你”或“样本”，移除机械的“用户”“样本对象”称呼。
- 生态日程中的行为重新遵守能量、关系、环境和冷却门控。

### 修复

- 修复跨进程读取桌面 ListView 时可能导致 Explorer 崩溃的问题，改用 Explorer 进程内远程缓冲安全读取。
- 修复图标赠予后出现在桌面左上角、单项“要回”无响应、背包已满仍接受赠予等问题。
- 修复光标隐藏后仍吸引角色视线、释放位置不在提包处、捕获失败形成伪没收等问题。
- 修复拖动窗口到屏幕顶部或左右边缘时角色被临时遮挡挤落的问题。
- 修复所站窗口关闭、隐藏或最小化后角色仍停在空中的虚空站立问题。
- 修复手动操控退出时残留旧窗口支撑，以及窗口可见片段重排造成角色横向瞬移的问题。

### 验证

- Godot 自动回归：1141 条断言全部通过。
- Windows x86_64 发行版成功导出并完成启动存活检查。
